Default development environment.  Inherits from `johntfoster/ipython`, but includes additional packages such as `cmake` and `vim`.  Pulls from `johntfoster/dotfiles` and `johntfoster/dotvim` for configuration.

